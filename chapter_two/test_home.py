"""
Write 3 separate test cases for every of shape object in `shapes.py` module and follow next steps:

  - Combine all test cases into one test suite and run it.
  - Combine test `area()` method from every test case into one test suite and run it.
  - Create 3 separate test suites for every of test cases, combine them into one top level test suite and run it.
"""

from chapter_two import shapes
